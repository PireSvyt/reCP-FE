/*import React from "react";
import ReactDOM from "react-dom";
import { act } from "react-dom/test-utils";
import Transaction from "../Transaction";
import ReactTestUtils from "react-dom/test-utils";*/

it("works", () => {
  expect(1 + 1).toBe(2);
});
/*
let mockedTransaction = ReactTestUtils.mockComponent(
  Transaction,
  "mockTagName"
);

it("peut afficher et mettre à jour un compteur", () => {
  // Teste le premier affichage et l'appel à componentDidMount
  //mockedTransaction.simulate.click;
  let node = ReactTestUtils.findRenderedDOMComponentWithTag(
    mockedTransaction,
    "tagName"
  );
  ReactTestUtils.Simulate.click(node);

  expect(document.title).toBe("Vous avez cliqué 1 fois");
});
*/
